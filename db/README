- Directory structure -
schema - contains DDL and deployment configuration
java - contains source code of stored procedures
- Compilation -
$ make all
Will compile Java code and create catalog.

$ make clean
Will do the cleanup and stop server (if running)

$ make run 
starts the server

$ make stop
stops the server
